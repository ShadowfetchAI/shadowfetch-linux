# Shadowfetch universal bootloader install. All logic (firmware detect, bios_grub
# type fix, i386-pc + x86_64-efi install, update-grub) lives in the script, which
# logs to /var/log/sf-install-grub.log on the target.
dontChroot: false
timeout: 700
script:
  - "sh /usr/local/sbin/sf-install-grub"
